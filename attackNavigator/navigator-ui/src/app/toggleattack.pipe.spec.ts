import { ToggleattackPipe } from './toggleattack.pipe';

describe('ToggleattackPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleattackPipe();
    expect(pipe).toBeTruthy();
  });
});
