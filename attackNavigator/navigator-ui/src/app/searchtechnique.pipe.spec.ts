import { SearchtechniquePipe } from './searchtechnique.pipe';

describe('SearchtechniquePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtechniquePipe();
    expect(pipe).toBeTruthy();
  });
});
