import { SearchattackPipe } from './searchattack.pipe';

describe('SearchattackPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchattackPipe();
    expect(pipe).toBeTruthy();
  });
});
