import { ToggledetectionPipe } from './toggledetection.pipe';

describe('ToggledetectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggledetectionPipe();
    expect(pipe).toBeTruthy();
  });
});
