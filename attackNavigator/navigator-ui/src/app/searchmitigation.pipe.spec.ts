import { SearchmitigationPipe } from './searchmitigation.pipe';

describe('SearchmitigationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchmitigationPipe();
    expect(pipe).toBeTruthy();
  });
});
